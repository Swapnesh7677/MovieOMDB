<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swapnesh.appmovie">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icons_movie"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icons_movie"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movies"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.MovieDetailsActivity"
            android:exported="true"
            android:theme="@style/Theme.Movies1"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
        <activity
            android:name="com.swapnesh.movies.view.ui.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Movies1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>